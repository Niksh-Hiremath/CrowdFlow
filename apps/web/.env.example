# Optional: override API origin used by next.config rewrites (server-side)
API_ORIGIN=http://127.0.0.1:8000

# WebSocket connects directly to the API (browser)
NEXT_PUBLIC_API_WS=ws://127.0.0.1:8000/api/sessions/{id}/sim/stream
